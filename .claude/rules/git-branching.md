When implementing a new feature, always work on a dedicated branch:

- Check if an appropriate branch already exists for the feature
- If no branch exists, create one before making any changes
- Use a descriptive branch name (e.g., `feat/feature-name`, `fix/bug-description`)
- Never make changes directly on `main`
